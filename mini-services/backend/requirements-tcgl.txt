# LearnHub Backend - Extra dependencies for TGCL ML model
# These are OPTIONAL. The app works with SM-2 + LaBSE without them.
# Install with: uv pip install -r requirements-tcgl.txt

torch>=2.0.0
torch-geometric>=2.4.0
